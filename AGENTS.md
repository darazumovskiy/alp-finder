# alp-finder

Поисково-аналитический проект: помощь штабу поисковой операции на пике Курумды (Кыргызстан) в отсмотре съёмок с дрона и вертолёта. Три альпиниста пропали при спуске после восхождения 2 августа 2026. Задача проекта — алгоритмическая предобработка десятков часов видео и выявление подозрительных кадров (люди, снаряжение, следы, палатки, верёвки) для передачи координат штабу.

**Это не учебный проект. От скорости и качества работы может зависеть жизнь людей.** Любая находка перепроверяется перед передачей штабу; ложные срабатывания тратят ресурс спасателей, пропущенные кадры — хуже.

## Контекст операции

Подробности: `docs/operation-context.md`. Ключевое:

- Район: пик Курумды (6096 м), Чон-Алайский хребет, восточнее пика Ленина.
- Критичный неосмотренный участок: между лагерями 2 и 3.
- Штаб выкладывает съёмку в общий Google Drive, контакты штаба в `docs/operation-context.md`.

## Структура проекта

```
AGENTS.md               — этот файл, точка входа
.cursor/skills/tochka-na-kartu-po-foto/ — скилл: добавление находки на карту по фото/скриншоту
                          (таймкод → SIFT-сверка кадра → телеметрия .gps.tsv → POINTS → деплой)
.cursor/skills/priyom-novykh-video/ — скилл: приём новых видео (Drive/Яндекс → телеметрия →
                          скан → ИИ-отсмотр → отчёт; слои покрытия карты; на карту не вносит)
docs/                   — вся накопленная информация; обновлять по мере рисерчей
  operation-context.md  — факты об операции, районе, группе, контакты
  drive-inventory.md    — полная опись Google Drive (id файлов, размеры, заметки)
  telegram/             — база знаний из TG-группы волонтёров, поддиректория на тему группы (см. telegram/README.md)
    svodka.md           — сводка находок и статусов по группе, обновлять при разборе
  nakhodki/             — реестр находок с координатами, кадрами (frames/), картой и KML для штаба;
                          search_vectors.kml — поисковый коридор по рельефу (внешний PR #3); линия доведена
                          до 4150 м, точка 4233 м из старого расчёта — промежуточная (расхождение снято 14.08)
  zadanie-dronu-2026-08-14.md — валидация пакета CC для оператора дрона (TG AI #1896–#1906):
                          что сошлось с реестром и что в задание добавить
  viewing-instructions.md — инструкция штаба по отсмотру (норматив; на неё ссылаются код и доки)
  nezavisimyy-analiz/   — пакет внешней группы анализа (волонтёр C C, PR #2): несмотренный коридор ниже
                          находок, GPS-ловушка EXIF, координаты вещей по дальномеру, 4 кандидата
                          (отсмотрены человеком 14.08, вердикты — analysis/review/nezavisimyye-kandidaty.md:
                          верёвка 5073 м подтверждена ув. 4, объект с белым пятном поднят до ув. 3)
  avalanche-assessment.md — спутниковый анализ (PR #3): крупная лавина 2–13.08 не подтверждается;
                          куда просить съёмку (ICEYE/Capella/Planet/Maxar) и что важнее спутников
  video-analysis.md     — конвейер анализа видео и статус пилота
  colour-signatures.md  — измеренные цвета найденных вещей и ловушек, колонки к выдаче детектора, пределы метода
  coverage-gsd.md       — реальное покрытие роликов 11–12.08 против порога 8 px: какие считать неосмотренными;
                          + слепое пятно склона между зоной интереса и вещами (полигон, analysis/coverage_polygon.py);
                          + сетка покрытия всего района по всем дням в 3 уровнях детальности
                          (analysis/coverage_map.py, слои «Осмотр: …» на карте)
  marshrut/             — плановый маршрут группы с карты штаба (GPX, лагеря) и сверка: зона интереса — на маршруте под Camp2
  focal-length-calibration.md — методика фокусного самокалибровкой (внешний PR #4)
analysis/               — код и артефакты анализа видео (venv, детектор, кропы); план — docs/video-analysis.md
  review/               — отчёты ручного отсмотра монтажных листов (по дням + вертолёт + углублённые)
  scene3d/              — 3D-модель участка находок плотной фотограмметрией (внешний PR #12, C C):
                          COLMAP + PatchMatch по клипу 163855, 1,54 млн точек, готовый просмотрщик —
                          docs/nezavisimyy-analiz/model-3d/model-3d.html; код — референс, не запускается
                          здесь без модулей автора (см. external/kurumdy-image-lab и README scene3d)
  viewer/               — статический HTML-просмотрщик: build_viewer.py (карточки кандидатов, монтажи),
 build_map.py (интерактивная карта: кандидаты с чекбоксами, маршрут, коридор,
 линии спуска по DEM, изолинии; слои покрытия дрона в 3 вложенных уровнях
 детальности с фильтрами «день / ролик» и ползунком плотности;
 расстояния до траектории падения в попапах;
                          точки с надёжным лучом пересажены с GPS дрона в проекцию центра кадра —
                          расчёт analysis/project_map_points.py, у остальных пунктирный ус
                          направления камеры).
                          Локальный запуск и развёртывание с нуля (venv, DEM) — README «Развёртывание».
                          Публичная копия: https://alp-finder.pages.dev (карта — /map); зеркало для
                          РФ/РБ, где *.pages.dev заблокирован провайдерами:
                          https://darazumovskiy.github.io/alp-finder/ (GitHub Pages, ветка gh-pages).
                          Обновление после перегенерации страниц: python analysis/viewer/build_dist.py
                          (собирает dist/ со всеми картинками, пути в страницах делает относительными),
                          затем оба деплоя:
                          npx wrangler pages deploy analysis/viewer/dist --project-name alp-finder
                          (Cloudflare Pages, логин через npx wrangler login)
                          и bash scripts/deploy_mirror.sh (зеркало)
data/drive/             — локальное зеркало Drive: <дата>/<слаг-папки>/<файл>
data/telegram/          — медиа из TG-группы: <слаг-темы>/<msg_id>_<файл>
data/dem/N39E073.tif    — тайл Copernicus GLO-30 для geoproject/coverage/карты; скачивание — README «Развёртывание»
external/               — клоны репозиториев коллег-волонтёров; в .gitignore, обновление — git pull внутри клона
  kurumdy-image-lab/    — репозиторий C C (https://github.com/ClemenceChee/kurumdy-image-lab):
                          его .py-агенты и кодовая база 3D-фотограмметрии, сопровождает analysis/scene3d;
                          клон: git clone https://github.com/ClemenceChee/kurumdy-image-lab.git external/kurumdy-image-lab
logs/                   — tg-updates.log (что и когда докачано из TG), tg-watch.log (служебный)
scripts/
  manifest.tsv          — манифест скачивания: локальный путь, drive file id, размер
  download_drive.sh     — идемпотентная докачка по манифесту
  drive_cron.sh         — ежечасный повтор докачки Drive (квоты Google); стоит в crontab
  tg_export.py          — выгрузка TG-группы (Telethon; сессия .tg_session — секрет, не публиковать)
  tg_cron.sh            — итерация докачки TG; стоит в crontab раз в минуту
  deploy_mirror.sh      — публикация dist/ просмотрщика в ветку gh-pages (зеркало для РФ/РБ)
```

## Данные

- Источник: общая папка Drive `1uD9uCzOm9e2GdKYiOO2Jp3GVvE8Cn3XX` (папки по датам). Доступ на чтение — публичный и через gdrive-коннектор.
- Локальное зеркало наполняется скриптом: `bash scripts/download_drive.sh` — качает по `scripts/manifest.tsv`, уже скачанные файлы (совпал размер) пропускает. При появлении новых файлов на Drive: обновить опись в `docs/drive-inventory.md`, дописать строки в манифест, перезапустить скрипт.
- SRT-файлы рядом с видео — телеметрия DJI: GPS, высота, параметры камеры на каждый кадр. Использовать для геопривязки кадров.
- Папки «Покадровый просмотр» на Drive (кадры, нарезанные волонтёрами) не скачиваются: кадры извлекаем сами из исходников; их разметка полезна как референс калибровки.
- Второй источник — Telegram-группа волонтёров «Анализ видео с дронам» (темы: новые скриншоты, гипотезы, подтверждённые/отклонённые кандидаты, инструкции). Выгрузка: `python scripts/tg_export.py` после разового логина (`--login`, нужны TG_API_ID/TG_API_HASH с my.telegram.org). Инструкция волонтёрам по отсмотру — `docs/viewing-instructions.md` (копия в базе TG: `docs/telegram/instrukcii-i-ssylki/2026-08-12-instrukciya-prosmotra.md`), «отклонённые» — готовый набор ложных срабатываний для калибровки.

## Правила работы

- Сначала инвентаризация и документирование, потом обработка: каждое видео должно иметь запись в описи (что снято, какой участок, есть ли телеметрия).
- Все выводы рисерчей фиксировать в `docs/` — проект рассчитан на много сессий, контекст живёт в файлах, не в чате.
- Кандидаты-находки оформлять с точной ссылкой: файл, таймкод, координаты кадра, скриншот, степень уверенности.
- Внешние PR (GitHub, волонтёр C C — он же автор расчётов General #1641/#1659 в TG) перед мержем проверять на наших данных: телеметрию — сверкой с волонтёрскими SRT (`data/telegram/general/1141.zip`), координаты — по записям дальномера в JPG (`LRFTarget*` в XMP). Влиты PR #1 (телеметрия с углами подвеса), #2 (`docs/nezavisimyy-analiz/`), #3 (`docs/avalanche-assessment.md` + `search_vectors.kml`), #4 (методика фокусного — только документ, код внешний), #5 (цветовые сигнатуры), #6 (стенд врезок `analysis/inject_probe.py`), #10 (запрос на облёт + оранжевый кандидат в `docs/nezavisimyy-analiz/`), #12 (3D-модель участка: `analysis/scene3d/` + `docs/nezavisimyy-analiz/model-3d/`).
